import ig from "../../../lib/impact/impact.js";
// Cards use crisp, resolution-independent typography and symbols. Assets registered
// here are loaded before the one long-lived Theseus game starts.
export const assets = Object.freeze({});
